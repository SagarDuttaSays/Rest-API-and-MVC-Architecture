const os = require('os')
console.log(os.arch()) //return x64 
//console.log(os.constants) //Returns an object containing the operating system's constants for process signals, error cotes etc.
//console.log(os.cpus()) //	Returns an array containing information about the computer's CPUs
console.log(os.userInfo())
console.log(os.type())
console.log(os.hostname())
